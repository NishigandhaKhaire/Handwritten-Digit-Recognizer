# Handwritten-Digit-Recognizer
A deep learning-based handwritten digit recognition system built using TensorFlow/Keras.  The model is trained on the MNIST dataset and integrated with a Flask web app for real-time predictions.
